@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<KHBPA.Models.Tag> tags)
    {
foreach (var tag in tags)
{
        <div class="tags-div">
            @KHBPA.Helpers.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
    }
}
